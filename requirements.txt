# Dependencias del proyecto ReminderMailVanatech
# Instalar con: pip install -r requirements.txt

# Integración COM con Microsoft Outlook (método de envío "outlook")
pywin32>=306

# Compilación a ejecutable Windows (.exe)
pyinstaller>=6.0
